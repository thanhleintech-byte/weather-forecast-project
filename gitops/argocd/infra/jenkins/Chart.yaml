apiVersion: v2
name: jenkins
description: Jenkins CI/CD — upstream public chart
type: application
version: 0.1.0
dependencies:
  - name: jenkins
    repository: https://charts.jenkins.io
    version: "5.5.0"
