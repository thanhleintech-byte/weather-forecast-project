apiVersion: v1
kind: Service
metadata:
  name: max-weather
  namespace: max-weather
  labels:
    app.kubernetes.io/name: max-weather
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: max-weather
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
